apiVersion: v1
kind: Service
metadata:
  name: spring-daily-lab
  labels:
    app.kubernetes.io/name: spring-daily-lab
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: spring-daily-lab
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
