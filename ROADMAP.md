# Product roadmap

자동 작업은 위에서 아래 순서와 현재 코드 상태를 함께 고려합니다. 각 항목은 독립적으로 설명·검증할 수 있을 때만 하나의 초안 PR로 진행합니다. 이미 열린 PR이 다루는 항목은 건너뜁니다.

## Phase 1 — API usability

- [ ] `API-001` 마감일 시작·종료 범위 필터를 추가하고 경계값 통합 테스트를 작성한다.
- [ ] `API-002` 제목과 설명에 대한 대소문자 무시 검색을 추가한다.
- [x] `API-003` 허용된 정렬 필드 목록을 적용해 잘못된 정렬 속성을 400으로 거부한다.
- [ ] `API-004` 대규모 목록용 커서 기반 페이지네이션 엔드포인트를 추가한다.
- [ ] `API-005` 여러 작업의 상태를 원자적으로 변경하는 벌크 API를 추가한다.
- [ ] `API-006` 완료 작업 보관과 보관 해제 기능을 추가한다.
- [ ] `API-007` 소프트 삭제·복원과 삭제 작업 조회 기능을 추가한다.
- [ ] `API-008` 작업 태그 엔티티와 태그 필터를 추가한다.
- [ ] `API-009` 작업 간 선행 조건을 모델링하고 순환 참조를 차단한다.
- [ ] `API-010` 반복 작업 규칙과 다음 작업 생성 기능을 추가한다.

## Phase 2 — Progress and reporting

- [ ] `PROG-001` 작업별 진행 기록 엔티티와 타임라인 API를 추가한다.
- [ ] `PROG-002` 작업에 실제 소요 시간을 기록하고 합계를 제공한다.
- [ ] `PROG-003` 연속 달성일 계산과 타임존 경계 테스트를 추가한다.
- [ ] `PROG-004` 일일 목표 수량·시간 설정과 달성률 API를 추가한다.
- [ ] `PROG-005` 주간 완료·미완료·기한 초과 보고서를 추가한다.
- [ ] `PROG-006` 월간 분류별 추세 집계를 추가한다.
- [ ] `PROG-007` CSV 내보내기와 필드 이스케이프 테스트를 추가한다.
- [ ] `PROG-008` JSON 백업 내보내기와 스키마 버전을 추가한다.
- [ ] `PROG-009` 가져오기 dry-run과 행 단위 오류 보고를 추가한다.
- [ ] `PROG-010` 사용자 지정 대시보드 위젯 설정을 저장한다.

## Phase 3 — Consistency and auditability

- [ ] `DATA-001` 생성 API에 Idempotency-Key 처리를 추가한다.
- [ ] `DATA-002` ETag와 If-Match 기반 낙관적 동시성 제어를 추가한다.
- [ ] `DATA-003` 생성·수정·삭제 감사 로그를 별도 테이블에 기록한다.
- [ ] `DATA-004` 감사 로그 보존 기간과 정리 작업을 추가한다.
- [ ] `DATA-005` 도메인 이벤트용 transactional outbox를 구현한다.
- [ ] `DATA-006` outbox 재시도와 실패 격리 정책을 추가한다.
- [ ] `DATA-007` Flyway 마이그레이션 네이밍 검사를 CI에 추가한다.
- [ ] `DATA-008` PostgreSQL Testcontainers 통합 테스트를 추가한다.
- [ ] `DATA-009` 데이터베이스 제약조건 회귀 테스트를 추가한다.
- [ ] `DATA-010` 백업·복원 절차와 검증 스크립트를 문서화한다.

## Phase 4 — Security

- [ ] `SEC-001` Spring Security 기본 구성을 추가하고 공개·보호 경로를 분리한다.
- [ ] `SEC-002` 계정 등록·비밀번호 해싱·로그인 기능을 추가한다.
- [ ] `SEC-003` 짧은 수명의 JWT access token과 갱신 흐름을 추가한다.
- [ ] `SEC-004` USER·ADMIN 역할 기반 권한 검사를 추가한다.
- [ ] `SEC-005` 작업 소유권을 도입해 사용자별 데이터 접근을 격리한다.
- [ ] `SEC-006` 로그인·쓰기 API rate limit과 429 응답을 추가한다.
- [ ] `SEC-007` 명시적 CORS allowlist 설정과 테스트를 추가한다.
- [ ] `SEC-008` 보안 응답 헤더와 캐시 정책을 추가한다.
- [ ] `SEC-009` 요청 본문·업로드 크기 제한과 초과 응답을 추가한다.
- [ ] `SEC-010` 운영 프로필에서 기본 비밀번호·약한 비밀값을 거부한다.

## Phase 5 — Operations and observability

- [ ] `OPS-001` Prometheus 메트릭 레지스트리와 scrape 엔드포인트를 추가한다.
- [ ] `OPS-002` 요청 correlation ID 생성·전파와 로그 연결을 추가한다.
- [ ] `OPS-003` JSON 구조화 로그 운영 프로필을 추가한다.
- [ ] `OPS-004` liveness·readiness 그룹과 데이터베이스 준비 상태를 검증한다.
- [ ] `OPS-005` 작업 생성·완료·기한 초과 도메인 메트릭을 추가한다.
- [ ] `OPS-006` 이메일 알림 포트와 로컬 fake 구현을 추가한다.
- [ ] `OPS-007` 서명된 webhook 전송과 재시도 정책을 추가한다.
- [ ] `OPS-008` 다중 인스턴스 스케줄러 락을 추가한다.
- [ ] `OPS-009` graceful shutdown 통합 테스트를 추가한다.
- [ ] `OPS-010` Docker Compose 장애 진단·복구 runbook을 작성한다.

## Phase 6 — Delivery and documentation

- [ ] `DEVX-001` OpenAPI 문서와 예제 요청·응답을 추가한다.
- [ ] `DEVX-002` API 오류 코드 카탈로그를 문서화한다.
- [ ] `DEVX-003` 주요 아키텍처 결정을 ADR 형식으로 기록한다.
- [ ] `DEVX-004` IntelliJ HTTP Client용 전체 API 예제를 추가한다.
- [ ] `DEVX-005` 한국어·영어 README 동기화 구조를 추가한다.
- [ ] `DEVX-006` 변경 로그 생성과 태그 릴리스 워크플로를 추가한다.
- [ ] `DEVX-007` CycloneDX SBOM 생성과 보관을 CI에 추가한다.
- [ ] `DEVX-008` 의존성 검토와 알려진 취약점 검사를 CI에 추가한다.
- [ ] `DEVX-009` 컨테이너 이미지 취약점 검사를 CI에 추가한다.
- [ ] `DEVX-010` Kubernetes Deployment·Service·ConfigMap 예제를 추가한다.
